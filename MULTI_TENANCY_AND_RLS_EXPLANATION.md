# 📖 دليل المعمارية الأمنية وإدارة الفروع المتعددة (Multi-Tenancy & RLS)
**مشروع:** Cyber Cafe Management System (CCMS)  
**التاريخ:** 2026-08-24  
**الموضوع:** شرح معمارية إضافة الكافيهات، فوائد العزل الأمني، وكيف تطور النظام من البداية حتى الآن.

---

## 🏢 1. كيف تعمل إضافة وإدارة الكافيهات بعد التعديلات؟

### 🔹 إضافة كافيه جديد (من صفحة Super Admin):
1. الدخول إلى لوحة السوبر أدمن عبر المسار: `/#/super-admin`
2. إدخال رمز المطور السري (`SUPER_ADMIN_KEY`).
3. ملء بيانات الكافيه الجديد:
   - اسم الكافيه (Tenant Name).
   - اسم المالك (Owner Full Name).
   - البريد الإلكتروني وكلمة المرور.
4. **الآلية الأمنية للعملية:**
   - يرسل التطبيق طلباً إلى الـ Backend (`POST /api/auth/register-tenant`).
   - يقوم السيرفر بإنشاء حساب المستخدم في Supabase Auth، وإنشاء سجل الكافيه في جدول `tenants` باستخدام صلاحيات السيرفر المشفرة (`cloudSupabase` مع Service Role).

---

### 🔹 تجربة كافيه جديد عند تسجيل الدخول:
بمجرد دخول المالك أو الكاشير الخاص بالفرع الجديد:
1. يصدر له السيرفر رمز دخول (JWT Token) مشفراً ومربوطاً بـ `tenant_id` الخاص بكافيهه الجديد حصراً.
2. تقوم المزامنة (`sync-engine.ts`) بجلب وتهيئة أجهزته وبياناته هو فقط.
3. **بفضل Migration 015:** سياسات الـ RLS السحابية تضمن العزل بنسبة 100%:
   - **الأجهزة، الجلسات، الفواتير، المصروفات، الورديات، وطلبات الكافيه** الخاصة به لا تظهر لأي كافيه آخر.
   - لا يستطيع هذا الكافيه رؤية أو تعديل أي بيانات تخص المقاهي الأخرى المشتركة في نفس قاعدة بيانات Supabase.

---

## 🌟 2. فوائد تعديل العزل الأمني (Migration 015)

| الفائدة | ما الذي تحقق وكيف يحميك؟ |
| :--- | :--- |
| **🛡️ 1. أمان وعزل كامل للفروع (True Multi-Tenancy)** | قاعدة البيانات أصبحت تطبق العزل على مستوى المحرك (Database-Level RLS). مستحيل لأي كاشير أو مسؤول في مقهى (فرع أ) أن يرى أو يعدل على بيانات مقهى آخر (فرع ب). |
| **💰 2. حماية الخصوصية والأسرار المالية** | أرقام المبيعات، الفواتير، المصروفات، وبيانات العملاء أصبحت مشفرة ومعزولة لكل مقهى على حدة. |
| **☕ 3. منع اختلاط طلبات الكافيه** | أصلحنا استعلام `session_orders` في المزامنة السحابية ليصبح كل فرع يسحب طلبات الكافيه المرتبطة بجلساته هو فقط. |
| **📦 4. اكتمال جداول وقواعد السحابة** | ضمنت Migration 015 إنشاء كافة الجداول الحديثة (تحويل الأجهزة، الورديات، المصروفات، المخزون، والغرف) بأعمدتها الصحيحة في Supabase دون أي نقص. |
| **🚀 5. الجاهزية للبيع كمنصة SaaS تجارية** | يمكنك الآن ربط عشرات الكافيهات المختلفة على نفس السحابة بثقة وأمان كامل دون أي خوف من تداخل البيانات. |

---

## ⏳ 3. كيف كان النظام يعمل قبل التعديلات؟

في بداية بناء المشروع، كان النظام مصمماً ليعمل على **مقهى واحد فقط (Single Tenant)**:

```
[مقهى أ] ─── (كود Node.js يطلب البيانات) ───> [قاعدة البيانات] (كل البيانات تتبع مقهى واحد)
```

1. **الاعتماد الكلي على كود السيرفر (Express API):**
   - عندما أضيفت ميزة الفروع المتعددة لاحقاً، أضيف عمود `tenant_id` وكان المبرمج يعتمد على وضع شرط الفلترة يدوياً في كود Node.js:
     ```typescript
     supabase.from('sessions').select('*').eq('tenant_id', req.user.tenant_id)
     ```
2. **استخدام المفتاح السري الخارق (`SERVICE_ROLE_KEY`):**
   - السيرفر كان يتصل بقاعدة البيانات باستخدام المفتاح السري الذي يتجاوز ويتجاهل أي سياسات أمان أصلاً، فكان التطبيق يعمل بدون أخطاء ظاهرة.

---

## ⚠️ 4. أين كانت الثغرات والمخاطر في الطريقة القديمة؟

1. **الخطأ البشري في الكود (Human Error):**
   - طالما أن الحماية لم تكن مفروضة داخل قاعدة البيانات نفسها، فلو نسي المبرمج كتابة `.eq('tenant_id')` في أي استعلام — وهذا **حدث بالفعل في كود المزامنة للسطر 390 لطلبات الكافيه `session_orders`** — فإن السيرفر كان يسحب طلبات كل الكافيهات بدون تصفية.
2. **سهولة اختراق البيانات السحابية مباشرة:**
   - منصة Supabase تتيح الاتصال المباشر بقاعدة البيانات عبر الـ API.
   - كانت السياسة القديمة في Supabase مكتوبة هكذا:
     ```sql
     -- ❌ السياسة القديمة (ثغرة):
     CREATE POLICY "staff read sessions" ON public.sessions 
     USING ( public.is_staff() );
     ```
   - هذا يعني: *"أي شخص يملك حساب موظف (حتى لو كان كاشير في كافيه آخر) افتح له كل الجلسات والفواتير لجميع الكافيهات!"*
   - فكان بإمكان أي شخص استخراج التوكن الخاص به واستعراض أرباح وفواتير كل المشتركين.

---

## ❓ 5. لماذا كان المطورون يتساهلون في الـ RLS سابقاً؟

في مراحل التطوير الأولى، يواجه المطورون صعوبات تقنية مع الـ RLS مثل:
* **ظهور مصفوفات فارغة `data: []`:** بسبب رفض السياسة للطلب عند عدم تطابق الـ Auth Token.
* **صعوبة كتابة الـ Joins والعلاقات المعقدة:** مثل ربط طلبات الكافيه بالجلسة ثم بالفرع.
* **الرغبة في إنجاز المميزات بسرعة:** فيلجأ المطور للحل الأسرع وهو كتابة شرط عام مثل `is_staff()` أو استخدام مفتاح الخدمة لتخطي قيود الأمان.

---

## 🛡️ 6. المعمارية الحالية: مبدأ الحماية متعددة الطبقات (Defense-in-Depth)

الآن أصبح النظام يطبق مبدأ الحماية المزدوجة:

```
                 ┌───────────────────────────────────────┐
                 │          طلب من كاشير كافيه (أ)       │
                 └──────────────────┬────────────────────┘
                                    │
                                    ▼
                 ┌───────────────────────────────────────┐
                 │       الطبقة 1: كود السيرفر API        │
                 │      (يفلتر الاستعلام بـ الفرع أ)     │
                 └──────────────────┬────────────────────┘
                                    │
                                    ▼
                 ┌───────────────────────────────────────┐
                 │     الطبقة 2: محرك قاعدة بيانات Supabase│
                 │  (RLS تتحقق إجبارياً: الصف = الفرع أ) │
                 └──────────┬──────────────────┬─────────┘
                            │                  │
               [الصف ينتمي للفرع أ]     [الصف ينتمي لفرع آخر ب]
                            │                  │
                            ▼                  ▼
                   ✅ إرجاع البيانات       🚫 حجب فوري 403
```

* **الطبقة الأولى (كود السيرفر):** يطلب بيانات الفرع.
* **الطبقة الثانية والأهم (محرك قاعدة البيانات RLS):** حارس أمني داخلي في Postgres يتحقق من كل صف يخرج أو يدخل: *"هل `tenant_id` لهذا الصف يطابق `tenant_id` الخاص بالمستخدم الحالي؟"* إذا لم يتطابق، تمنعه قاعدة البيانات أوتوماتيكياً حتى لو حدث خطأ في كود السيرفر.

---

## 📋 7. الإجراءات المتبقية لتجهيز النظام للإنتاج

1. **تدوير المفتاح السري (`SUPABASE_SERVICE_ROLE_KEY`):**
   - من **Supabase Dashboard** ➔ **Settings** ➔ **API** ➔ **Rotate Key**.
   - تحديث السطر 4 في ملف `.env` بالمفتاح الجديد.
2. **إعادة تشغيل السيرفر المحلي:**
   - لتفعيل كود المزامنة الجديد وقراءة المفتاح المحدث.
3. **تجربة إنشاء فرع جديد:**
   - الدخول إلى `/#/super-admin` وإنشاء صالة جديدة واختبار تسجيل الدخول بها للتأكد من نظافة وعزل البيانات.
